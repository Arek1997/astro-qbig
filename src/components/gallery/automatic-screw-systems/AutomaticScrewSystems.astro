---
import Accordion from "../accordion/Accordion.astro";
import ImageWrapper from "../image-wrapper/ImageWrapper.astro";

const images = await Astro.glob(
  "/public/images/gallery/automatic-screw-systems/*"
).then((files) => files.map((file) => file.default));
---

<Accordion title="Systemy automatycznego dokręcania">
  {
    images.map((image, index) => (
      <ImageWrapper
        image={image}
        alt={`Image nr-${index + 1}`}
        dataGalleryImageNumber={index}
        dataGalleryName="automatic-screw-systems"
      />
    ))
  }
</Accordion>
